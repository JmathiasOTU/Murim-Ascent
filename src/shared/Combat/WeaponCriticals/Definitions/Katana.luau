--!strict
-- PLACEHOLDER BALANCE — tune freely, unlike the animation ids these aren't exact values.

return {
	HitboxSize = Vector3.new(6, 6, 7.5),
	HitboxOffset = CFrame.new(0, 0, -4.5),

	BaseDamage = 20,
	StunDuration = 1.2,

	WindupTime = 0.70,
	ActiveTime = 0.2,
	EndlagTime = 0.9,
	Cooldown = 0,
}
