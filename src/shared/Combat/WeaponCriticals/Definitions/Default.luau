--!strict
-- Fists

return {
	HitboxSize = Vector3.new(4.5, 5, 5),
	HitboxOffset = CFrame.new(0, 0, -3.25),

	BaseDamage = 12,
	StunDuration = 1.0,

	WindupTime = 0.45,
	ActiveTime = 0.15,
	EndlagTime = 0.6,
	Cooldown = 8,
}
